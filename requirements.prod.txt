fastapi>=0.115.0
uvicorn[standard]>=0.30.0
anthropic>=0.45.0
# [http2] is a direct need (socrata.py passes http2=True); don't rely on
# qdrant-client happening to pull h2 in transitively.
httpx[http2]>=0.27.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
qdrant-client>=1.12.0
sentence-transformers>=3.2.0
shapely>=2.0.0
python-dotenv>=1.0.0
aiosqlite>=0.20.0
python-multipart>=0.0.7
Pillow>=10.0.0
beautifulsoup4>=4.12.0
lxml>=5.3.0
PyJWT>=2.8.0
sentry-sdk[fastapi]>=2.0.0
matplotlib>=3.8.0
# Direct import in the report renderer (main.py); previously installed only as
# torch's transitive dependency.
jinja2>=3.1.0
weasyprint>=60.0
stripe>=8.0.0
