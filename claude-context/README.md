# Context Docs — UrbanLayer

Load only what you need for the current task. Don't read everything.

## Core (load 1-2 for most coding tasks)
- `core/architecture.md` — RAG pipeline, domain orchestrators, vector search pipeline, key design decisions
- `core/data-sources.md` — All datasets (Socrata, ArcGIS, external APIs), endpoint reference, PIN system
- `core/known-issues.md` — Active bugs, known limitations, fragile heuristics, gotchas, operational status

## Property Discovery (filter/search workbench — SHIPPED in 2 waves, dark)
- `property-discovery/` — Engineering handoff spec + implementation record. **Status: LIVE ON PRODUCTION (2026-06-14, three waves) — no longer dormant.** Nav-linked; prod index built for **25 community areas (~482k parcels)**, coverage `partial`, recipes serving real counts (teardown 902, vacant_mf_transit 548, undervalued_mf 31, …). Wave 1 (2026-06-13, `f192d57`) = invariant core/compilers/evaluator + list-first FE. Wave 2 (2026-06-14, `8c279c6`) = the `result.rows` workbench (PR1–PR10): rows wire + single `_resolve`, 32 filters + labels + range metadata + 6 recipes, coverage/populatedFields cold-start, scannable list + infinite scroll, deck.gl map (`/search/pins`), premium CSV (`/search/export`), query-first recipe-shelf, server-enforced free teaser, mobile+a11y. **Wave 3 (2026-06-14, merges `78294d8`+`8d3ae07`+`731aca6`) built the index + launched it:** PR-INDEX derived fields (`value_percentile`/`upside_score`/`is_teardown_candidate`/transit) + `populated_fields` manifest; the **CCAO valueless-latest-year assessment fix** (0%→99% — ⚠️ likely lurks in scorecard/report too); **recipe-count honesty** (`recipeCounts` → "Live · N"/"No matches yet", kills LIVE-but-empty); **3-tier addresses 28%→99%** (own → building base-PIN → nearest-approx `~`); **index persistence** on the `backend/data` volume; self-activating nav-link (`coverage != none`); + perf (memoized `_pin_lookup`), PR-VAL (`index_validate.py` — upside is plausible v1, NOT validated), monthly rebuild timer. **⚠️ Known limit: `--all` (1.8M) OOMs the 8 GB box (in-container build spike) — expand by CA set; needs off-box build for full city.** **Start with `10-implementation-status.md`** (Wave 3 section: decisions, lessons, known problems). Normative spec docs 00–09 unchanged (drift: `03` says 29 filters — live is 32; `07` wire is `{rows,total,nextOffset,gated}` + `/search/pins` + `/search/export`). Implementation: `backend/discovery/` + `frontend/src/discovery/`. **Direction: Discovery is a strict superset of `/explore` with real data now → retire `/explore`** (redirect, drop page + `/api/explore*`). Strategy/why: `strategy/property-discovery-filters.md`.

## Guides (load when working in a specific area)
- `guides/backend-guide.md` — Router, assembler, synthesizer, caching, HTTP clients, concurrency, testing, eval
- `guides/frontend-guide.md` — Components, design tokens, state management, map system, responsive, animations
- `guides/auth-and-conversations.md` — Full auth flow (Google OAuth + JWT), conversation persistence, sharing
- `guides/deployment.md` — Server ops, .env template, deploy commands, monitoring, database backups
- `guides/latency-reduction.md` — Completed + planned perf optimizations, pipeline timing reference
- `guides/zoning-cache.md` — **Precomputed zoning-extraction cache (2026-06-18, COMMITTED on `fix/report-oom` `9840d37`, NOT deployed).** Why the report's AI zoning extraction was silently failing (partial-chunk retrieval of 30K-char Title-17 bulk tables → mostly `low`/null → silent table fallback), and the fix: offline deterministic **full-section fetch** + **hybrid merge** (table authoritative for FAR/height/coverage, AI adds setbacks/min-lot) → 57/59 high-confidence, 0 FAR errors, reranker gone from the report path. Section map, rebuild command, known limits (setbacks uncross-validated, parking deferred), and **remaining work (live render not yet done, deploy pending)**. Read before touching zoning extraction or the report's zoning section.
- `guides/report-v5-plan.md` — V5 Report plan (shipped 2026-06-10). Reference for understanding synthesis rules, envelope rendering spec, approval pathway logic.
- `guides/report-v4-plan.md` — V4 Report plan (items 1-7 shipped; item 8 superseded by V5)
- `guides/report-status.md` — **Master report feature tracker**: shipped (V3-V6), blocked/data-dependent, confirmed limitations, rejected features, future ideas
- `guides/report-v6-improvements.md` — V6 open issues: parcel map needs real geometry, year_built data gaps, ownership limitation, comps map validation
- `guides/report-v6-audit.md` — **Rejection audit of `report_v6g.pdf`** (2026-06-10): every finding ranked by decision-quality impact × effort, quadrant view, execution order, verified root causes. Key caveat: audited PDF was mock=true — regenerate real-data before fixing
- `guides/report-v6-execution-plan.md` — **Master V6 phased execution plan & status.** Phase 1 (viability: R1 zoning fallback, R2 tax/assessment, R3 comps, R4 formatting) **SHIPPED 2026-06-10 (commit f0c1996)** with verified results; Phases 2–4 (credibility, decision-quality, UX/viz) pending. Verification parcels: EX subject `14283190070000` + taxable control `14331030110000`. Read this first for current report work.

## Strategy (load only for product planning / business discussions)
- `strategy/north-star.md` — **North Star Product Plan**: manifesto, wedge (per-unit reports), feature audit, development phases, customer validation plan. This is the governing strategy document.
- `strategy/product-coherence-audit.md` — **Product Coherence Audit (2026-06-11, post-SelectedParcel)**: founder-level first-principles review. Verdict: one product ("parcel dossier machine" — find/open/interrogate/buy), front door wired to the wrong room. Funnel map, per-surface coherence scorecard, future-state conceptual architecture. Steps 1 (renaming + bridge), 2 (map defaults + pricing), and 3 (address-first homepage + auth off the front door + Phase 2 sequencing) SHIPPED 2026-06-11/12; **Step 4 (2026-06-15) IN WORKING TREE, not yet shipped** — see `2026-06-15_homepage-coherence-pass.md` and §10. Read alongside north-star.md for any product/UX/funnel work.
- `strategy/2026-06-15_homepage-coherence-pass.md` — **Step 4 of the coherence audit (2026-06-15), IMPLEMENTED IN WORKING TREE, NOT committed/pushed/shipped.** Address-only hero (chat fully off the front door); ScorecardPage two-shell state model + code-question failure-recovery handoff; nav trim (Analyst removed, About unlinked); vocabulary canonicalization (Scorecard/Report/analyst); in-session "New Chat" reset-in-place + empty state. Records both the decisions and the **reusable reasoning frameworks** (focused-funnel-not-launcher; chat "distinct in kind, unequal in weight"; design vs. user-visible vs. user-harming inconsistency; distinct-modes over omnibox) + what was **deferred** (cold-start chat, visual unification, homepage Pricing). Read before any homepage/entry/chat-access work.
- `strategy/phase2-interview-kit.md` — **Phase 2 customer-validation interview kit (2026-06)**: recruiting channels + outreach template per segment, expanded interview script, observation checklist, per-interview log template, demo-day pre-flight (cold-load latency, rate limits, known data quirks verified against prod 2026-06-12). Companion to north-star Phase 2.
- `strategy/competitive-analysis.md` — Chicago Cityscape comparison, data gaps, structural weaknesses
- `strategy/property-discovery-filters.md` — **Filter/search strategy/why** (2026-06-13; the system is now **SHIPPED** — see `property-discovery/10-implementation-status.md` for implementation reality): candidate filters, 6-filter MVP (class, neighborhood, zoning group, incentive, vacancy, lot size), prioritization, rollout. Part A = architecture-independent product answer; Part B = downstream "prospecting index" note (now built for MVP fields); Part C = self-critique on why the index is a dependency, not a premise. Verdict ⚠️ on the original architecture-led framing.
- `strategy/product-roadmap.md` — Revenue features, pricing model, target personas, open questions
- `strategy/design-guidelines.md` — Hybrid dashboard+chat model, UX principles, when to use chat vs tools

## Archive (historical — don't load unless asked about past decisions)
- `archive/2026-06-16_report-oom-reranker.md` — **`/api/report` 504 incident (2026-06-16)**: real cause was the **reranker hanging `semantic_search`** (>40–60s/search × 5 parallel in `extract_zoning_standards`), NOT the OOM first suspected. Fix = `RERANKER_ENABLED=false`. Also shipped: subprocess-isolated PDF render (`backend/report_render.py`, ~118 MB), swap 2→8 GB, nginx `/api/report` 180s, discovery-index interning, **zoning markdown-fence fix** (AI extraction had been silently off). Reverted: torch-threads reranker re-enable (hypothesis disproven). **Resolved 2026-06-18:** reranker fix (`e59990b`, bounded concurrency + 20-batch) profiled + **verified on prod — still too slow (40s/search, 280s report path, CPU-bound not swap)**, so rolled back; report path **decoupled from the reranker via a precomputed zoning cache** (`9840d37`, not yet deployed) — see `guides/zoning-cache.md`. Active truths live in `core/known-issues.md`.
- `archive/2026-06-12_scorecard-ux-phases.md` — Scorecard UX overhaul phases 1-4 (shipped & deploy-verified 2026-06-12): pin→address backfill (`pin_to_address`), trust copy/badges, progressive disclosure + grid reorder, `zone_definition` in /api/scorecard + ZoningCard, verdict line, static map thumbnail, sample report + CTA rewrite + `sample_report_click`. Post-ship watch: investigate_click volume, sample→CTA→purchase funnel.
- `archive/2026-06-12_homepage-and-auth.md` — Coherence audit step 3 (shipped 2026-06-12): address-first hero (AddressInput/HeroEntrance → `/scorecard?address=`, librarian chat secondary), anon chat opened (gate removed, 3/day IP limit, in-memory only, 429 UX fixed), conversation endpoints `require_auth` + PATCH ownership check, anon CSRF-cookie bootstrap on `/api/auth/me`, persona intent-routing, dead landing demo code deleted, Phase 2 sequencing decided (homepage before interviews).
- `archive/2026-06-12_map-defaults-and-pricing.md` — Coherence audit step 2 (shipped 2026-06-12): map defaults flipped (crime/311/permit dots off + intent-gated auto-enable, transit on), pricing page rebuilt as 3-card with the $25 report leading, landing footer Pricing link, es reportCTA translation fix.
- `archive/2026-06-11_rename-and-bridge.md` — Coherence audit step 1 (shipped 2026-06-11): "report" reserved for the paid artifact (chat export → "transcript", landing copy points at the $25 report), chat→Scorecard bridge (ScorecardBridgeCard + clickable ReportTeaser + `scorecard_bridge_click` event).
- `archive/2026-06-11_selected-parcel.md` — SelectedParcel unification (shipped 2026-06-11): frontend parcel-identity primitive, pin-keyed handoffs + canonical `?pin=` URLs, PIN-bound purchases (db schema v11), SelectedParcel-typed report API. Includes the full spec, verification results, and binding findings.
- `archive/2026-06-09_a-la-carte-reports.md` — A la carte $25 report purchases via Stripe one-time payment (code complete 2026-06-09)
- `archive/2026-06-08_i18n-spanish.md` — Full i18n implementation plan (shipped 2026-06-08)
- `archive/2026-06-05_deployment.md` — Phase-by-phase deployment log (all phases complete)
- `archive/2026-06-05_expansion-phases.md` — Completed expansion phases, Tier 3 decisions
- `archive/2026-06-07_revenue-sprint.md` — 4-feature sprint plan + implementation details (all shipped)

## Keeping Docs Current
After completing work, update the relevant files to reflect changes. If you added backend modules,
update `backend/CLAUDE.md`. Frontend changes → `frontend/CLAUDE.md`. New data sources →
`core/data-sources.md`. New bugs/issues → `core/known-issues.md`.

## Archivation Rules
When a feature, plan, or initiative is **fully shipped to production**, archive it:

1. **Create an archive file** using the template at `archive/TEMPLATE.md`. Name it `YYYY-MM-DD_short-name.md`.
2. **Strip the completed content** from active files — remove strikethrough items, "Done" statuses, shipped phase narratives, and fully-implemented plans. Keep only the one-line summary of what shipped (e.g., in a "Completed" table row or changelog line).
3. **Add the archive entry** to this README's Archive section.
4. **Don't archive partial work** — if 3 of 5 items shipped, archive the 3 and leave the 2 in the active file.
5. **Known-issues.md cleanup** — when a bug is fixed, move its entry from `core/known-issues.md` to the archive file for the feature that fixed it. Don't leave strikethrough "Fixed" entries in the active file.
