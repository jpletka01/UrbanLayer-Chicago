# Resolver investigation (2026-06-21) — RESOLVED

The diagnosis evidence behind the PIN-resolution seam fix (two resolvers disagreeing; dense-block row-cap
truncation evicting the true-nearest parcel). Conclusion and fix shipped — see
`archive/2026-06-21_pin-resolution-seam.md`; the canonical contract is `guides/parcel-resolution-truth-model.md`.
Historical marker only.
