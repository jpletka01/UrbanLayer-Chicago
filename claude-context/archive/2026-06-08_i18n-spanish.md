# i18n — Spanish (2026-06-08) — SHIPPED

Full internationalization shipped: English (default) + Spanish. The **architecture principle** — backend stays
English-only (DB, Qdrant, city APIs never translated); translation happens at exactly two boundaries: the LLM
synthesizer outputs in the user's language, and the React frontend localizes static UI strings.

Living mechanics (namespaces, parity guards, how to add a language, the localized $25 report via
`report_i18n.py`) are in **`frontend/CLAUDE.md` → i18n** and **`backend/CLAUDE.md`**. The original 5-language
ambition (Polish/Chinese) was scoped down to en+es. Historical marker only.
