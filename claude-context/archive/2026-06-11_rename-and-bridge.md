# Coherence audit step 1 — rename + bridge (2026-06-11) — SHIPPED

"Report" reserved for the paid artifact (chat export → "transcript"; landing copy points at the $25 report);
chat→Scorecard bridge (`ScorecardBridgeCard` + clickable `ReportTeaser` + `scorecard_bridge_click`).
Step 1 of the product-coherence audit (steps 1–4 all shipped). Full funnel narrative:
`strategy/product-coherence-audit.md`. Historical marker only.
