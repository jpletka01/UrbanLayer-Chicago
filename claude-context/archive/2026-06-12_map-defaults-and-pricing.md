# Coherence audit step 2 — map defaults + pricing (2026-06-12) — SHIPPED

Map defaults flipped (crime/311/permit dots off + intent-gated auto-enable, transit on); pricing page rebuilt as
a 3-card layout with the $25 report leading; landing-footer Pricing link. The current map-default logic is the
**living** `frontend/CLAUDE.md → Map layer defaults`. Historical marker only.
