# SelectedParcel unification (2026-06-11) — SHIPPED

Frontend parcel-identity primitive: pin-keyed handoffs + canonical `?pin=` URLs, PIN-bound purchases (DB schema
v11), a `SelectedParcel`-typed report API (hand-constructed report identity is a compile error). The truth-model
that governs who may write parcel identity is the **living** doc `guides/parcel-resolution-truth-model.md`;
full narrative is on the About page → **Parcel Identity**. Historical marker only.
