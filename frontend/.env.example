# Mapbox GL access token (https://account.mapbox.com/access-tokens/)
VITE_MAPBOX_TOKEN=pk.your_token_here

# Backend API base URL. The backend runs on :8001 (see README "Run").
VITE_API_BASE=http://localhost:8001
