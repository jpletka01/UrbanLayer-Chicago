{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "gauntlet/schemas/proposals.json",
  "title": "Improvement proposals (FR-6.3)",
  "description": "The cheap-model synthesis output: concrete, path-contained diffs against versioned assets. Each proposal targets a SINGLE allowlisted asset — a prompt/pipeline/schema or the judge policy, by its exact repo-relative path under the repo's asset root — and carries the literal unified diff plus a rationale. The engine re-validates path-containment (review F-001) and `git apply --check`s every diff before any human is asked to approve it (FR-6.4); a proposal never self-applies.",
  "type": "object",
  "additionalProperties": false,
  "required": ["proposals"],
  "properties": {
    "proposals": {
      "type": "array",
      "items": {"$ref": "#/definitions/proposal"}
    }
  },
  "definitions": {
    "proposal": {
      "type": "object",
      "additionalProperties": false,
      "required": ["slug", "target_path", "rationale", "diff"],
      "properties": {
        "slug": {
          "type": "string",
          "description": "Short kebab-case identifier for the proposal, e.g. `triage-rubric-false-bikeshedding`."
        },
        "target_path": {
          "type": "string",
          "description": "Repo-relative path of the single versioned asset this diff edits. Must be the exact repo-relative path of an allowlisted asset as shown to the synthesizer (a prompt/pipeline/schema or the judge policy.yaml, carrying any .gauntlet/ prefix); anything else is rejected at parse time (review F-001)."
        },
        "rationale": {
          "type": "string",
          "description": "Why this change helps, traced to the feedback / retrospective evidence that motivated it."
        },
        "diff": {
          "type": "string",
          "description": "The literal unified diff in `git apply` format (---/+++/@@ hunks), repo-relative paths, touching only `target_path`."
        }
      }
    }
  }
}
